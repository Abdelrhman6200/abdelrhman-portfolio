# Visual Verification Notes

## Reference-driven refinement

- Desktop homepage: the route-like hero evidence, project index, and recurring trace markers sit within the existing dark command surface and paper archive without changing the main section order.
- Desktop demos: both `/demo/cgf` and `/demo/questionnaire` now expose `SYSTEM // 001` in the sticky header and a visible `TRACE / LIVE` handoff strip connecting input, transformation, and output.
- Typography: display accents in the major headings and demo titles now use the Space Grotesk-led control-room hierarchy; sage marks stable state and signal yellow remains tied to active/selected actions.
- Mobile homepage: hero route items stack cleanly, project index collapses to one column, and project architecture previews remain readable without horizontal overflow.
- Mobile demos: both forms stack into readable panels, the handoff strip wraps into a compact trace, and sticky headers remain legible at 375px width.
- Validation: TypeScript check and production build completed successfully. The build continues to emit only the existing bundle-size advisory.
